import { AppPageSkeleton } from "@/components/shared/app-page-skeleton";

/** Premier rendu / navigation vers la racine avant redirection. */
export default function Loading() {
  return <AppPageSkeleton />;
}
